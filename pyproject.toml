[project]
name = "kis-mcp-trading"
version = "0.1.0"
description = "Fetch the live current price of Samsung Electronics (005930) via the KIS Open API"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "websockets>=16.1.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
